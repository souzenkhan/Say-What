Measured Latency: < 500 ms (startup delay)

Observation:
Audio playback begins almost immediately after pressing Play, with minimal noticeable delay.

Limitation:
This measurement reflects playback startup time, not real-time audio transmission latency.

Future Work:
Implement live audio streaming (e.g., WebRTC) to measure and reduce real-time latency.
